# config/patches/subnets.patch.yaml
machine:
  kubelet:
    nodeIP:
      validSubnets:
        - 10.0.0.0/24
cluster:
  etcd:
    advertisedSubnets: # listenSubnets defaults to advertisedSubnets if not set explicitly
      - 10.0.0.0/24
